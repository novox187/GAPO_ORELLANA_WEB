node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Restos del asistente de fase 1, que corría dentro de este contenedor. Se
# quedan ignorados un tiempo para que a quien tenga el repo clonado no le
# aparezcan como archivos sin seguimiento tras hacer pull.
src/lib/server/corpus.generado.json

# Pesos del modelo de embeddings. Ya no se descargan aquí —el modelo vive en
# el microservicio del backend—, pero la carpeta sigue ignorada por lo mismo.
modelos/
